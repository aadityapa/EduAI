/**
 * Placeholder until `pnpm openapi:generate` is run against live services.
 * Generator writes `{service}.openapi.json`, `{service}.ts`, and replaces this index.
 */
export type IdentityPaths = Record<string, never>;
export type LearningPaths = Record<string, never>;
export type AiPaths = Record<string, never>;
export type ErpPaths = Record<string, never>;
export type BillingPaths = Record<string, never>;
